groups:
  - name: app_health
    rules:
      - alert: AppDown
        expr: probe_http_status_code == 0
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "App is Down!"
          description: "The cleaningbiz-server is not responding to /health"